Atividade Individual: MANIPULAÇÃO DE STRINGS - CONTAINS

1) DEFINIÇÃO DO CONTAINS (o que é ? / para que serve?)
R:É um método de string. Este método é usado para verificar se o caracter ocorre dentro de uma determinada string ou não.
